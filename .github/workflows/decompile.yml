name: Extract Kernel Offsets

on:
  push:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  offsets:
    runs-on: ubuntu-latest
    timeout-minutes: 240

    env:
      DEVICE: "iPhone14,5"
      IOS_BUILD: "24A437"
      IOS_VERSION: "27.0"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: temurin

      - name: Setup Ghidra
        uses: antoniovazquezblanco/setup-ghidra@v2.0.6
        with:
          version: "11.0.3"

      - name: Install ipsw (snap)
        run: |
          set -euo pipefail
          sudo snap install ipsw
          ipsw version

      - name: Download IPSW
        run: |
          set -euo pipefail
          mkdir -p "${GITHUB_WORKSPACE}/ipsw"
          ipsw download ipsw \
            --device "${DEVICE}" \
            --build "${IOS_BUILD}" \
            --output "${GITHUB_WORKSPACE}/ipsw" \
            --confirm
          IPSW="$(find "${GITHUB_WORKSPACE}/ipsw" -type f -name '*.ipsw' -print -quit)"
          if [[ -z "${IPSW}" ]]; then
            echo "ERROR: no IPSW downloaded"
            exit 1
          fi
          echo "IPSW=${IPSW}" >> "${GITHUB_ENV}"
          echo "[+] IPSW=${IPSW}"

      - name: Extract Kernelcache
        run: |
          set -euo pipefail
          mkdir -p "${GITHUB_WORKSPACE}/kernel"
          ipsw extract --kernel "${IPSW}" --output "${GITHUB_WORKSPACE}/kernel"
          KERNEL="$(find "${GITHUB_WORKSPACE}/kernel" -type f \( -name 'kernelcache*' -o -name '*.kernelcache' \) -print -quit)"
          if [[ -z "${KERNEL}" ]]; then
            echo "ERROR: no kernelcache found"
            exit 1
          fi
          echo "KERNEL=${KERNEL}" >> "${GITHUB_ENV}"
          echo "[+] KERNEL=${KERNEL}"

      - name: Clone Symbolicator signatures
        run: |
          set -euo pipefail
          git clone --depth 1 https://github.com/blacktop/symbolicator.git "${GITHUB_WORKSPACE}/symbolicator"

      - name: Symbolicate Kernelcache
        run: |
          set -euo pipefail
          ipsw kernel symbolicate \
            --signatures "${GITHUB_WORKSPACE}/symbolicator/kernel" \
            "${KERNEL}"
          SRC="${KERNEL}.symbols.json"
          if [[ ! -f "${SRC}" ]]; then
            echo "ERROR: ${SRC} not produced"
            ls -la "$(dirname "${KERNEL}")"
            exit 1
          fi
          cp "${SRC}" "${GITHUB_WORKSPACE}/symbols.json"
          echo "[+] symbols.json size:"
          wc -c "${GITHUB_WORKSPACE}/symbols.json"

      - name: Cache Ghidra project
        id: cache-project
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/ghidra_project
          key: ghidra-project-24A437-iphone14-5-v1

      - name: Run Ghidra headless (warm)
        if: steps.cache-project.outputs.cache-hit == 'true'
        env:
          _JAVA_OPTIONS: "-Xmx6g -Djava.awt.headless=true"
        run: |
          set -euo pipefail
          GHIDRA_ROOT="${GHIDRA_INSTALL_DIR}"
          echo "[+] Warm start — reusing cached analysis"
          "${GHIDRA_ROOT}/support/analyzeHeadless" \
            "${GITHUB_WORKSPACE}/ghidra_project" KernelProject \
            -process "kernelcache.release.iPhone14,5" \
            -noanalysis \
            -postScript decompile.py \
            -scriptPath "${GITHUB_WORKSPACE}/scripts" \
            2>&1 | tee "${GITHUB_WORKSPACE}/ghidra.log"

      - name: Run Ghidra headless (cold)
        if: steps.cache-project.outputs.cache-hit != 'true'
        env:
          _JAVA_OPTIONS: "-Xmx6g -Djava.awt.headless=true"
        run: |
          set -euo pipefail
          GHIDRA_ROOT="${GHIDRA_INSTALL_DIR}"
          echo "[+] Cold start — full analysis"
          mkdir -p "${GITHUB_WORKSPACE}/ghidra_project"
          "${GHIDRA_ROOT}/support/analyzeHeadless" \
            "${GITHUB_WORKSPACE}/ghidra_project" KernelProject \
            -import "${KERNEL}" \
            -postScript decompile.py \
            -scriptPath "${GITHUB_WORKSPACE}/scripts" \
            -max-cpu 4 \
            -analysisTimeoutPerFile 9000 \
            2>&1 | tee "${GITHUB_WORKSPACE}/ghidra.log"

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-offsets-${{ github.sha }}
          path: |
            ${{ github.workspace }}/offsets.txt
            ${{ github.workspace }}/symbols.json
            ${{ github.workspace }}/ghidra.log
          retention-days: 30
          if-no-files-found: warn
